<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.weblink.zbcommunity">

    <!-- AndroidImageSlide权限-->
    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- 高德地图 -->

    <!--用于进行网络定位-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"></uses-permission>
    <!--用于访问GPS定位-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"></uses-permission>
    <!--用于获取运营商信息，用于支持提供运营商信息相关的接口-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <!--用于访问wifi网络信息，wifi信息会用于进行网络定位-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
    <!--用于获取wifi的获取权限，wifi信息会用来进行网络定位-->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"></uses-permission>
    <!--用于访问网络，网络定位需要上网-->
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <!--用于读取手机当前的状态-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"></uses-permission>
    <!--用于写入缓存数据到扩展存储卡-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <!--用于申请调用A-GPS模块-->
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS"></uses-permission>
    <!-- 访问网络，网络定位需要上网-->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SD卡读取权限，用户写入离线定位数据-->

    <!--相机权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.hardware.camera"></uses-permission>
    <uses-permission android:name="android.hardware.autofocus"></uses-permission>
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!--android:theme="@android:style/Theme.Holo.Light.NoActionBar"-->
    <application
        android:name=".ZBApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.Light.NoActionBar"
        tools:replace="android:icon, android:theme">

        <service android:name=".utils.LocationService">
            <intent-filter>
                <action android:name="com.test.SERVICE_TEST" />
                <category android:name="android.intent.category.default" />
            </intent-filter>
        </service>


        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="70562b5a2e655e067557ee407038a700" />


        <service android:name="com.amap.api.location.APSService"></service>

        <!-- 主界面-->
        <activity android:name=".MainActivity" />
        <!-- 登录-->
        <activity android:name=".activity.LoginActivity" />
        <!-- 修改密码-->
        <activity android:name=".activity.ChangeActivity" />
        <!-- 用户信息-->
        <activity android:name=".activity.MuserinforActivity" />
        <!-- 我的优惠券-->
        <activity android:name=".activity.MinecoActivity" />
        <!-- 帮助与反馈-->
        <activity android:name=".activity.MinehelpActivity" />
        <!-- 订单-->
        <activity android:name=".activity.MyorderActivity" />
        <!-- 评价-->
        <activity android:name=".activity.EvaluateActivity" />
        <!-- 注册-->
        <activity android:name=".activity.RegisterActivity" />
        <!-- 引导页-->
        <activity android:name=".activity.GuideActivity" />
        <!-- 选择位置-->
        <activity android:name=".activity.SelectLocActivity" />
        <!-- 商品超市-->
        <activity android:name=".activity.CommkindActivity" />
        <!-- 常见问题-->
        <activity android:name=".activity.MinehelpxxActivity" />
        <!-- 订单商品清单-->
        <activity android:name=".activity.MineThingList" />
        <!-- 搜索商品-->
        <activity
            android:name=".activity.SearchActivity"
            android:launchMode="singleTask" />
        <!-- 搜索结果-->
        <activity android:name=".activity.SearchResultActivity" />
        <!-- 频道页面-->
        <activity android:name=".activity.ChannelActivity" />
    </application>


</manifest>